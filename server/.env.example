PORT=5000
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/quickbiz
JWT_SECRET=
CORS_ORIGIN=
NODE_ENV=production

# NOTE: Google Cloud Vision has been removed.
# OCR is performed on-device using Google ML Kit Text Recognition v2.
# No service-account JSON or GOOGLE_APPLICATION_CREDENTIALS is needed.
